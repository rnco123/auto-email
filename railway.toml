[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/api/health/supabase"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
